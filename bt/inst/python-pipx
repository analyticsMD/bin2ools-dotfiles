
source <( echo "$("${BT}"/cmd/_path -s 2>/dev/null)" )

echo -ne "\n\n----------------------------------\n"
echo -ne     "installing pipx deps...           \n"
echo -ne     "----------------------------------\n\n"

pipx ensurepath

echo -ne "\n\ninstalling pipx packages.\n"

pipx install awsume
pipx inject  awsume awsume-console-plugin
pipx inject  awsume awsume-default-profile-plugin
pipx install aws-sso-util
pipx install aws-whoami 

echo -ne "\n\n--------------------------\n" 
echo -ne     "python packages installed:\n" 
echo -ne     "--------------------------\n\n" 

# NOTE: experimental.
pipx install -f aws-fuzzy-finder
#pipx inject devops-sso-util  virtualenv

# local installs
#declare -a packages 
#declare -a apps


pipx uninstall devops-sso-util || true
pipx uninstall bin2ools-ssm || true
pipx uninstall bin2ools-rds || true

# install devops and b2 namespace packages.
u="git+https://github.com/analyticsmd"
p="devops-sso-util"
g="@master#subdirectory"
opts="-f --include-deps"
pipx install $opts "devops-sso-util @ $u/$p$g=cli&devops-sso-util"

opts="-f --include-apps --include-deps"
pipx inject $opts devops-sso-util "b2ssm @ $u/$p$g=b2ssm&ssm_cmpl&ssm_gen&ssm_local"
pipx inject $opts devops-sso-util "b2rds @ $u/$p$g=b2rds&rds_cmpl&rds_gen&rds_local&dg&rds_refresh"

#virtualenv --help

pipx list
echo "LAST pipx command!"
pipx completions
